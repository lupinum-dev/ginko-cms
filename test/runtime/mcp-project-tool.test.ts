import { describe, it } from 'vitest'

describe.skip('test_runtime_mcp_project_tool_test_ts', () => {
  it('is disabled during Phase 1 hard cutover', () => {
    // TODO(trellis-cutover): restore or replace this coverage in the stabilization phases.
  })
})
