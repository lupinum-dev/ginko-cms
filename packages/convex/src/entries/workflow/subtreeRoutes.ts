/**
 * Legacy descendant projection rewriting was intentionally deleted.
 *
 * Public paths are now derived from the publication tree, so publishing or
 * moving one node changes descendant URLs atomically without rewriting their
 * rows or appending synthetic revisions. This empty module remains only until
 * the checked-in Convex component declaration is regenerated.
 */
export {}
